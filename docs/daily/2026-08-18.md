# GitHub Trending 日报 · 2026-08-18

## 概览

今日共收录 **11** 个 Trending 项目。主要语言分布：Python(4)、TypeScript(3)、Rust(3)、JavaScript(1)。



## 项目详情

### [harry0703/MoneyPrinterTurbo](https://github.com/harry0703/MoneyPrinterTurbo)

 `Python` ⭐ +1,189 今日新增 · 106,166 总星数

> 输入主题或关键词，自动生成脚本、匹配素材、配音、字幕并合成高清短视频。解决短视频制作耗时问题，近期因 AI 视频生成热度和厂商赞助而爆火。

**亮点**

- 把 LLM、TTS、视频素材 API 和 FFmpeg 拼成一个无人值守的自动化流程，用户只需给主题，后续全部自动完成，降低短视频制作门槛。
- 模型和素材源高度可插拔：支持十几家 LLM 提供商、七种 TTS、三个免费视频素材站，避免被单一厂商锁定。

**核心功能**

- 内置从文案到成片的完整流水线：LLM 生成脚本和素材搜索词，Pexels/Pixabay API 拉取免版权素材，Edge TTS 等合成配音，FFmpeg 合成字幕和背景音乐并输出 1080p 视频。
- 提供 WebUI、API、CLI、AI Agent 四种交互方式，支持批量生成和分辨率选择（9:16 竖屏 / 16:9 横屏）。
- 一键跨平台发布：生成后可直接上传到 TikTok、Instagram、YouTube Shorts。

**适用场景**: 自媒体运营、短视频创作者、营销人员需要快速批量产出短视频时，用该工具把选题变成成品视频，省掉剪辑和配音环节。

**竞品对比**: 对比同类的 ShortGPT、MoneyPrinter 等开源项目，MoneyPrinterTurbo 多了 WebUI 和跨平台发布，开箱即用程度更高。

**成熟度**: 较成熟：10 万+ star，功能丰富且持续更新，但依赖外部 AI 服务和素材站 API，可能需要自己配密钥。

**趋势信号**: AI 生成短视频工具正处于风口，加上 Kimi、火山引擎等厂商赞助推广，以及一键出片的低门槛体验，让 star 在近期猛涨。

---

### [cordiverse/cordis](https://github.com/cordiverse/cordis)

 `TypeScript` ⭐ +957 今日新增 · 5,630 总星数

> Cordis 是 Koishi 机器人的底层框架，用依赖注入和生命周期管理把插件像积木一样拼起来，让你不用管组合顺序。

**亮点**

- 整个框架核心只有几千行 TypeScript，没有任何运行时依赖，但实现了从服务注册到上下文隔离的完整元框架能力
- 采用『上下文』对象传递作用域，让每个插件都拿到独立的环境，插件升级或卸载时不会污染全局状态

**核心功能**

- 用依赖注入容器自动解析插件间的服务依赖，插件之间只通过服务接口协作，不需要直接引用对方
- 统一管理异步生命周期，插件启动/停止时自动触发对应钩子，避免内存泄漏和资源未释放
- 服务标识用 Symbol 而非字符串，从根本上杜绝命名冲突，插件可以无缝覆盖或扩展已有服务

**适用场景**: 做机器人框架或复杂应用插件系统的开发者，尤其是需要大量插件动态加载、相互调用的场景，比如 Koishi 生态里的功能模块扩展。

**竞品对比**: 相比 NestJS 这类重型 DI 框架，Cordis 更轻、更偏重插件组合而非企业级分层，且原生支持异步生命周期和热重载。

**成熟度**: 生产可用，作为 Koishi 的底层已稳定运行多年，有活跃社区和完整文档。

**趋势信号**: Koishi 近期发布新版带动了底层框架关注度，加上机器人开发社区升温，Cordis 作为核心依赖被大量转发。

---

### [usestrix/strix](https://github.com/usestrix/strix)

 `Python` ⭐ +598 今日新增 · 54,244 总星数

> 开源 AI 渗透测试工具，用 LLM 驱动自主代理动态扫描应用漏洞，生成 PoC 和修复补丁。解决传统扫描器误报多、人工渗透慢的问题。因 AI Agent 安全测试趋势而热门。

**亮点**

- 用真正的 PoC 验证漏洞，而非传统静态分析规则匹配，大幅降低误报率。
- Agent 能驱动浏览器和 HTTP 代理，像真实黑客一样动态利用漏洞，同时自动生成修复补丁。

**核心功能**

- 基于 LLM 的多代理编排，多个 AI 代理协作执行侦察、利用、验证，动态运行目标代码而非仅静态扫描。
- 集成真实渗透工具链：HTTP 拦截代理（Caido）、浏览器自动化、自定义 Python exploit 沙箱，生成可复现的 PoC。
- 可直接接入 CI/CD（GitHub Actions），PR 时自动扫描并阻止不安全代码合并；提供 SKILL.md 让 Claude Code、Cursor 等编码代理调用。

**适用场景**: 开发者和安全团队在 CI/CD 中快速检测漏洞，或安全研究员用于自动化漏洞赏金研究，替代人工渗透测试的前期工作。

**竞品对比**: 相比 Semgrep、Snyk 等静态扫描工具，Strix 动态验证漏洞并产出 PoC；相比传统商业渗透测试服务，它自动化、低成本且集成 CI/CD。类似项目有 Prynt，但 Strix 多代理编排和平台化更成熟。

**成熟度**: 快速迭代期，核心功能可用，已有托管平台和企业集成，但开源 API 可能变化。

**趋势信号**: AI Agent 渗透测试是当前安全领域热点，项目提供免费开源版和托管平台，结合 LLM 驱动的自主代理概念，吸引大量开发者试用。

---

### [agalwood/Motrix](https://github.com/agalwood/Motrix)

 `TypeScript` ⭐ +344 今日新增 · 53,115 总星数

> Motrix是一个全功能下载管理器，基于Electron和Aria2，支持HTTP、FTP、BT、磁力等协议，界面简洁且跨平台，目前因社区推荐再次受到关注。

**亮点**

- 采用Aria2作为下载引擎，UI层完全自研，既保留了Aria2的高性能和扩展性，又提供友好图形界面
- 通过每日自动更新tracker列表和端口映射功能，显著提升BT下载的连通性和速度，这是多数下载工具没有的细节

**核心功能**

- 内嵌Aria2核心，支持HTTP/FTP/BT/磁力下载，单任务最多64线程，可同时跑10个任务
- 每天自动更新BT tracker列表，提升种子下载速度，并支持UPnP/NAT-PMP端口映射
- 用Electron+Vue构建跨平台桌面UI，覆盖Windows、macOS、Linux，支持暗黑模式和系统托盘速度显示

**适用场景**: 需要免费、无广告、跨平台下载工具的用户，比如经常下载BT资源、磁力链接或普通文件的普通用户，也适合开发者下载大文件或GitHub发布包。

**竞品对比**: 对比迅雷和Internet Download Manager（IDM），Motrix完全开源免费、无广告、跨平台，但缺少一些高级功能如浏览器集成插件（需手动添加）

**成熟度**: 成熟稳定，已发布多年，版本超过1.8，5万星和大量发行版包维护说明生产可用。

**趋势信号**: 近期无明显版本大更新，可能是因技术社区或社交平台推荐而重新走红，具体原因不明。

---

### [santifer/career-ops](https://github.com/santifer/career-ops)

 `JavaScript` ⭐ +218 今日新增 · 64,707 总星数

> Open-source AI job search: scan job portals, evaluate listings with a structured A-F rubric into a 1.0-5.0 score, tailor your CV, track applications — runs locally in your AI coding CLI (Claude Code, Codex, OpenCode, Antigravity…)

---

### [akitaonrails/ai-memory](https://github.com/akitaonrails/ai-memory)

 `Rust` ⭐ +207 今日新增 · 2,105 总星数

> 用 Rust 写的 AI 编程代理长期记忆工具，跨会话、跨代理（Claude Code、Codex 等）保留上下文，避免重复解释架构和失败方案。

**亮点**

- 跨代理厂商的标准化记忆层，用统一格式捕获并恢复会话，而非绑定单一 AI 工具。
- 采用生命周期钩子（如 SessionStart/Stop）而非轮询，减少性能开销，并能精确控制捕获范围。

**核心功能**

- 通过 MCP + 生命周期钩子自动捕获会话内容，支持 20 多种 CLI/IDE 代理，如 Claude Code、Codex、Gemini CLI、Cursor 等。
- 用 SQLite 存储结构化记忆，提供 session-aware 隔离和最终摘要生成，支持手动 finalize-session 触发交接。
- 原生 Rust 实现，支持 Linux/macOS/WSL2，Windows 实验性支持；提供 Docker 镜像、AUR 包和多平台二进制。

**适用场景**: 开发者从 Claude Code 切到 Codex 或其它 AI 编码工具时，在同一目录继续任务，无需重新解释项目背景；也适合多代理协作或长时间编码任务的上下文交接。

**竞品对比**: 相比各代理自带的会话记忆（如 Claude 的 Projects），它独立于具体厂商，支持任意组合切换，并与 MCP 生态兼容。

**成熟度**: 已发布 1.x 版本，支持矩阵广，提交活跃，但 Windows 原生仍实验性，插件生态部分依赖社区维护。

**趋势信号**: AI 编码代理爆发式增长，用户同时使用多个 CLI，跨代理切换时上下文丢失是刚需痛点；该项目直接解决『换工具就要重新解释』的问题，符合当前多代理工作流趋势。

---

### [mukul975/Anthropic-Cybersecurity-Skills](https://github.com/mukul975/Anthropic-Cybersecurity-Skills)

 `Python` ⭐ +198 今日新增 · 28,470 总星数

> 817 structured cybersecurity skills for AI agents · Mapped to 6 frameworks: MITRE ATT&CK, NIST CSF 2.0, MITRE ATLAS, D3FEND, NIST AI RMF & MITRE F3 (Fight Fraud) · agentskills.io standard · Works with Claude Code, GitHub Copilot, Codex CLI, Cursor, Gemini CLI & 20+ platforms · 29 security domains · Apache 2.0

---

### [AlexsJones/llmfit](https://github.com/AlexsJones/llmfit)

 `Rust` ⭐ +198 今日新增 · 32,316 总星数

> 一个 Rust 写的终端工具，自动检测你的硬件（内存、CPU、GPU），从几百个模型里挑出能流畅跑的，并给出速度和质量评分。现在火是因为它推出了实机基准测试功能，让估算变成实测数据。

**亮点**

- 社区基准数据众包：用户实测数据通过 PR 合入，下个版本里同配置硬件直接显示实测值，不用自己再跑
- Windows 二进制通过 SignPath 签名，安装包可信度较高，这在开源 CLI 工具里很少见

**核心功能**

- 用内存带宽模型估算推理速度，每条估算都列出假设依据，并支持通过`llmfit bench`实际测量 tok/s 后回填到评分表
- 内置多后端支持（Ollama、llama.cpp、MLX、Docker Model Runner、LM Studio），能针对 MoE 模型计算激活参数而非总参数，内存估算更准
- 提供 TUI 和 CLI 双模式，CLI 可输出 JSON 供脚本或 Agent 调用，还支持多 GPU 和动态量化选择

**适用场景**: 本地跑 LLM 的开发者或运维，想快速知道自己的笔记本/工作站能流畅运行哪些模型，避免下错模型白等；也适合写自动化脚本批量评估多台机器的模型适配性。

**竞品对比**: 对比同类工具 llm-checker（Node.js，通过 Ollama 实跑），llmfit 主打估算+社区实测数据，无需预装 Ollama，且支持 MoE 架构的精确内存计算。

**成熟度**: 已发布 1.0 版本，crates.io 可安装，提供 Homebrew/Scoop/安装脚本，CI 和签名流程完善，属于生产可用早期阶段。

**趋势信号**: 本地 AI 热潮下，人人都在折腾模型但硬件参差不齐，llmfit 正好解决‘我到底能跑啥模型’的普遍痛点，且刚发布实测基准功能，带动了一波讨论。

---

### [immich-app/immich](https://github.com/immich-app/immich)

 `TypeScript` ⭐ +175 今日新增 · 111,192 总星数

> 给想自建照片备份的人用的 Google Photos 替代品，支持手机自动备份、人脸识别和 AI 搜索，在 GitHub 上已经 11 万星。

**亮点**

- 把机器学习模型（人脸识别、CLIP）都做成可选组件，低配设备也能用，高配设备能跑完整功能
- 采用 AGPLv3 协议，但文档和社区很完整，翻译成 20 多语言，全球用户多

**核心功能**

- 把 CLIP 模型跑在自家机器上，可以用自然语言搜照片，比如搜'狗在海滩'，不依赖云端 API
- 人脸识别加聚类，自动把同一个人归到相册，支持命名和合并
- 移动端后台自动备份，支持 LivePhoto、RAW 格式，还能跨设备同步

**适用场景**: 家里有 NAS 或服务器的数码爱好者，不想把照片放 Google 或 iCloud 的人。用 Docker 或直接装上，连接手机就能自动备份。

**竞品对比**: 对比 Photoprism 和 LibrePhotos，Immich 功能更贴近 Google Photos，比如手机端后台备份和 LivePhoto 支持，而且更新速度很快。

**成熟度**: 生产可用，但还在快速迭代，API 可能变动，建议备份好数据再升级。

**趋势信号**: Google Photos 不再免费无限存储，加上自托管运动兴起，用户把隐私和照片控制权当回事，Immich 成了首选替代品。

---

### [nautechsystems/nautilus_trader](https://github.com/nautechsystems/nautilus_trader)

 `Rust` ⭐ +120 今日新增 · 25,953 总星数

> Rust 原生的生产级交易引擎，用事件驱动架构让回测和实盘共用同一套代码，Python 做策略控制，适合量化交易和 AI 训练。

**亮点**

- 采用 Soundness Pledge，承诺无内存安全漏洞，用 Rust 的类型系统和所有权模型保证线程安全和数据竞争-free。
- 支持 Redis 做状态持久化，可以跨进程重启恢复，适合交易系统需要高可靠的场景。

**核心功能**

- 核心用 Rust 编写，通过 PyO3 绑定给 Python 用，内置 mimalloc 分配器和 tokio 异步网络，处理订单和行情延迟极低。
- 同一套事件驱动引擎和确定性时间模型同时用于回测和实盘，策略无需改代码即可从研究部署到生产，避免回测失真。
- 模块化适配器架构，已接入多家加密交易所（CEX/DEX）、外汇、股票、期货和博彩交易所，能同时跑多市场策略。

**适用场景**: 量化基金或个人交易者做高频或做市策略，需要在相同代码基础上先回测再实盘；也用于强化学习等 AI 交易代理训练，因为引擎够快能扛住大量模拟。

**竞品对比**: 相比 vn.py 和 QuantConnect Lean 这类 Python 交易框架，NautilusTrader 的 Rust 核心性能更高，回测/实盘一致性设计更彻底；比 Freqtrade 更专业，支持复杂订单类型和做市。

**成熟度**: v2 处于 RC 阶段，但 version 标签显示已在生产环境使用，文档完整，社区活跃，有企业用户，整体接近生产可用。

**趋势信号**: 近期 AI 交易代理（RL/ES）训练需求爆发，NautilusTrader 宣传自己引擎够快能用于训练，且加密市场持续火热，带动了关注度。

---

### [jundot/omlx](https://github.com/jundot/omlx)

 `Python` ⭐ +78 今日新增 · 19,011 总星数

> 一个面向Apple Silicon的本地LLM推理服务器，支持连续批处理和内存/SSD分级KV缓存，可通过macOS菜单栏管理，提供仪表板和OpenAI兼容API。

**亮点**

- KV缓存分热冷两层（内存+SSD），对话上下文变了也能复用历史缓存，这是其他本地推理工具没做到的
- 菜单栏直接管理，能常驻内存、按需换模型，还内置离线仪表板和8种语言，日常使用体验接近原生App

**核心功能**

- 用MLX做推理，分层KV缓存把常用token放内存、冷数据放SSD，上下文切换不用重算，长会话省显存
- 自动扫描模型目录，识别文本/视觉/OCR/嵌入/重排序模型，跑起来就是OpenAI兼容API，无需额外适配
- 支持多Mac分布式推断，用Ring或Thunderbolt RDMA拆分模型，能跨内存不等的Mac协同跑大模型

**适用场景**: macOS开发者想在本地跑LLM给Claude Code等工具当后端，又不想折腾命令行，要能随时切换模型、保持上下文。

**竞品对比**: 比Ollama、LM Studio更‘Mac原生’，有菜单栏和分级缓存，但对非Apple硬件没有支持。

**成熟度**: 功能丰富但部分能力（如自定义内核、多Mac分布式）需额外构建，生产环境建议用官方DMG。

**趋势信号**: 本地LLM在Mac上越来越流行，oMLX用菜单栏+SSD缓存解决了Ollama等不擅长长会话和系统集成的问题，被开发者社区推荐。

---

## 趋势观察


